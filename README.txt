I created nav tags, multiple section tags, a footer tag, and an aside tag.

I added comments to the CSS to mark where formatting occurs, images are added, and where images are modified.

I added alt texts to the HTML images.